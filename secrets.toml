# secrets.toml
# THIS FILE SHOULD NOT CONTAIN REAL SECRETS IN THE REPOSITORY.
# The original Google API key was removed because it was leaked publicly.
# Keep a local copy only (this file should be in .gitignore).
[google]
api_key = "REDACTED_REMOVED_FOR_SECURITY"
